# Test Configuration

# TestContainers configuration will be used dynamically in tests

# Logging for tests
logging.level.com.recruitment.complaints=DEBUG
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG

# Disable schema generation by Hibernate (Liquibase will handle it)
spring.jpa.hibernate.ddl-auto=validate